## idea
- `$(".faq-question").on("click", fn)` 처럼 각각에 걸면 동작은 하지만, 나중에 동적으로 추가된 요소에는 이벤트가 안 걸린다.
- 부모 하나에 걸고 위임 선택자로 자식을 지정하면(`.on("click", ".faq-question", fn)`), 클릭 버블링을 이용해 부모가 처리 → 새로 추가된 자식도 자동으로 동작한다.